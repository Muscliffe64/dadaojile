# 打到几了 · 部署与上线说明

适用版本：本压缩包内代码（含组队方式可选、套圈双勾选、按牌局门槛、DeepSeek AI 报告等）。

---

## 一、文件结构概览

```
掼蛋争霸赛统计/
├── app.js / app.json / app.wxss          全局入口与样式
├── sitemap.json                           索引规则
├── project.config.json                    微信开发者工具项目配置（appid 待填）
├── pages/                                 小程序页面
│   ├── index/                             首页（牌局选择 / 排行榜 / 最近对局）
│   ├── record/                            记录本局（组队方式 / 套圈双勾选）
│   ├── history/                           历史记录
│   ├── leaderboard/                       排行榜
│   ├── players/                           参与人管理
│   ├── tables/                            牌局管理
│   ├── import/                            数据导入 / 导出 / 重置
│   ├── api-config/                        设置（API Key / 排行榜门槛）
│   ├── report-config/                     AI 报告配置
│   ├── report-list/                       AI 报告历史
│   └── report-view/                       AI 报告详情
├── utils/
│   ├── storage.js                         本地存储读写、统计逻辑
│   ├── importCsv.js                       CSV 解析
│   ├── aiReport.js                        DeepSeek API 调用
│   └── config.js                          预留全局配置
├── images/                                静态图片
├── server/                                ⚠️ 已废弃（旧版 Node 中转服务器，小程序直连 DeepSeek 后不再需要）
└── DEPLOY.md                              本文档
```

---

## 二、第一次打开项目（开发环境）

### 1. 安装并打开微信开发者工具
- 下载地址：<https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html>
- 建议使用 Stable Build 1.06+ 版本。

### 2. 导入项目
1. 启动开发者工具，登录你的微信开发者账号。
2. 选择 **「小程序」→「+」→「导入项目」**。
3. **项目目录**：选择本压缩包解压后的根目录（包含 `app.json` 的那一层）。
4. **AppID**：
   - 如果你已经在微信公众平台申请了小程序 AppID，填入它（推荐）。
   - 若仅本地预览测试，可先选「测试号」。**正式上线前必须替换为正式 AppID**。
   - AppID 的修改入口：开发者工具左上角项目名 → 详情 → 项目设置 → AppID。
5. **项目名称**：默认即可（"打到几了"）。
6. 点击「确定」，进入项目主界面。

### 3. 检查依赖与编译
- 项目纯小程序原生开发，**无需 npm install / 无需 node_modules**。
- 打开后开发者工具会自动编译，左侧模拟器即可看到首页。

---

## 三、必须的小程序后台配置

### 1. 服务器域名白名单（AI 报告功能必需）

DeepSeek API 调用经由 `wx.request`，微信要求请求域名必须在小程序后台白名单里。

1. 访问 <https://mp.weixin.qq.com>，用小程序管理员账号登录。
2. **开发管理 → 开发设置 → 服务器域名**。
3. 在 **request 合法域名** 中添加：
   ```
   https://api.deepseek.com
   ```
4. 保存。每月修改次数有限制，谨慎操作。

> 临时调试可在开发者工具中：**详情 → 本地设置 → 勾选「不校验合法域名…」**，绕过线上校验。**正式版必须在后台配置白名单**。

### 2. 业务域名（可选）
本项目无 `web-view`，可不配置。

---

## 四、关键功能配置

### DeepSeek API Key
1. 注册并登录 DeepSeek：<https://platform.deepseek.com>
2. 在控制台创建 API Key，复制 `sk-xxxxxxxx...`。
3. 在小程序中：**首页 → 底部"设置"链接 → DeepSeek API Key 卡片**，粘贴并保存。
4. Key 仅保存在用户本地（`wx.storage`），不会上传服务器。

### 排行榜门槛（可选）
- 默认每个牌局都是 0（即所有玩家都入榜）。
- 进入"设置"页 → 排行榜门槛 → 滑轮选牌局 + 输入局数 + 点"添加" → 该牌局即按设定值过滤。
- 已设置的会出现在"已设置"列表，点"移除"恢复为 0。

### 组队方式（记录本局）
- 三选一：`1+2 vs 3+4` / `1+3 vs 2+4` / `1+4 vs 2+3`
- 仅影响占位号码的显示提示，不影响保存的数据，可放心切换。
- 选择会自动记忆到下次。

---

## 五、本地测试流程建议

按这个顺序点一遍主要功能，确保都正常：

1. **参与人管理** → 添加 4 人以上。
2. **牌局管理** → 至少 1 个牌局（默认已有"默认牌局"）。
3. **记录本局** → 选组队方式 → 选 4 人 → 选获胜方 → 选比分级数 → （可选）勾选套圈 → 保存。
4. **历史记录** → 看到刚保存的对局；尝试删除一条。
5. **首页** → 总局数、本周、连胜、最近对局是否显示正常。
6. **排行榜** → 多记几局后排名是否正确。
7. **数据导入/导出** → 导出当前 CSV 复制粘贴；新建一个空牌局，把刚才复制的内容粘贴回去测试导入与去重。
8. **设置页** → 调整某牌局门槛，回首页/排行榜验证文案与排名变化。
9. **AI 报告**（需先配 API Key + 开发者工具勾选"不校验合法域名"）→ 选牌局、选侧重、点生成。

---

## 六、上传 / 上线步骤

### 1. 上传体验版
1. 开发者工具右上角 **「上传」** 按钮。
2. 填写「版本号」（建议语义化，例如 `1.0.0`）和「项目备注」（这一版改了什么）。
3. 上传完成后，前往 <https://mp.weixin.qq.com> → 版本管理 → 开发版本，能看到刚上传的版本。

### 2. 设置体验版
- 在「开发版本」一栏点「选为体验版」。
- 在「成员管理」中添加体验者微信号；体验者搜索小程序名即可使用。

### 3. 提交审核 / 发布
1. 体验版自测通过后，在「开发版本」点「提交审核」。
2. 按平台提示填写「功能页面」、「小程序类目」（建议：工具 / 效率工具）。
3. **提交审核备注里建议写明（关于 AI 内容审核）**：

   > 本小程序为掼蛋（一种扑克牌游戏）对局统计工具，用于个人和好友圈子记录比分、查看胜率排行。
   >
   > "AI 分析报告"为可选功能：
   > - 仅当用户主动配置自己的 DeepSeek API Key 并主动点击"生成报告"时才会调用。
   > - 调用方为用户本人 Key，结果仅展示在用户自己的设备上，不上传到任何第三方服务器。
   > - 报告内容基于用户自己输入的对局数据生成，无 UGC 不涉及向他人传播。
   > - 已在前台对生成内容标注"AI 生成内容仅供娱乐参考"。

4. 通常 1–2 个工作日内审核完成。如被驳回（之前出现过"含有 AI 不可上传"），再追加申诉说明上面那段话，必要时补充截图证明 AI 入口需用户主动配置 Key。

### 4. 发布
- 审核通过后，在版本管理 → 审核版本里点「发布」即正式上线。

---

## 七、常见问题

### Q: 真机调试时 `wx.request` 报"url not in domainList"
A: 域名未配置白名单。开发者工具勾选"不校验合法域名"可临时绕过；正式上线必须在小程序后台 → 服务器域名里加 `https://api.deepseek.com`。

### Q: 保存了 API Key 但仍提示"请先配置 API Key"
A: 早先版本（暂停 AI 时）有此问题。本版本已修复，`utils/aiReport.js` 直接读取 `wx.storage` 中的 Key。

### Q: 历史对局格式 `A2:A2 1队套圈 2队套圈` 是什么意思
A: 比分字段是字符串：左右为 1 队/2 队级数（2…A1/A2/A3，A 三局制），后接空格分隔的"X 队套圈"标记。新版套圈支持双方都打勾，最多两个标记同时出现。

### Q: 想恢复"全局默认入榜局数"
A: `utils/storage.js` 中 `setMinGamesForRank` / `getDefaultMinGames` 仍保留，仅 UI 移除。需要时可在设置页加回输入框，并把 `getMinGamesForRank` 的逻辑改回回落到全局默认。

### Q: server/ 目录还需要部署吗
A: 不需要。当前小程序直连 DeepSeek，不依赖中转服务器。`packOptions.ignore` 已配置打包时排除该目录。

---

## 八、版本变更速览（vs 上一发布版）

- 记录本局：新增"组队方式"选择（1+2 / 1+3 / 1+4 三种），占位号码动态切换。
- 记录本局：套圈改为双勾选，1 队 / 2 队可同时打勾。
- 记录本局：取消 / 保存按钮文字垂直居中修复。
- 设置页：每个牌局可独立设置入榜门槛；未设默认 0。
- 设置页：DeepSeek API Key 输入框、提示文字优化显示。
- AI 报告：恢复 DeepSeek 真实调用（OpenAI 兼容协议）。
- 数据交互：每个区块加简短描述、字段对照表；重置卡片新增"建议先导出"提醒。

---

如需进一步改动，建议保留本文档同步更新。

