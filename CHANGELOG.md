# 打到几了 · 版本更新说明

## v1.20（本次）

发布日期：__________  
上一版本：v1.10

---

### 📋 项目备注（可直接复制到微信小程序后台「上传 → 项目备注」框）

> v1.20 更新：
> 1. 记录本局新增"组队方式"选择（1+2 / 1+3 / 1+4），占位编号跟随真实抽签号显示。
> 2. 套圈支持双方独立勾选，可同时记 1 队和 2 队套圈。
> 3. 排行榜入榜门槛改为按牌局独立设置，未单独设置默认 0（所有玩家入榜）。
> 4. 设置页重做：UI 显示更完整、用滑轮选牌局 + 添加按钮的交互。
> 5. AI 报告功能恢复（接入 DeepSeek API，需用户自行配置 Key）。
> 6. 数据交互页补充字段说明与重置前导出提醒。
> 7. 修复：取消 / 保存按钮文字居中。

> 体验版可用菜单：首页 → 记录本局 / 牌局管理 / 数据交互 / 设置 / AI 分析报告。

---

## 🆕 新增

### 1. 记录本局：组队方式可选
- 增加"组队方式"切换，三选一：
  - `1+2 vs 3+4`（默认）
  - `1+3 vs 2+4`
  - `1+4 vs 2+3`
- 切换后 1 队 / 2 队的占位号码会动态变化（例如选 `1+3 vs 2+4` 时，1 队两个槽显示"1号 / 3号"，2 队显示"2号 / 4号"）。
- 选择会自动记忆到下次进入。
- 数据结构未变，旧记录不受影响。

### 2. 记录本局：套圈双勾选
- 原"无 / 1队套圈 / 2队套圈"三选一 picker，改为两个独立勾选项：「1队套圈」「2队套圈」，可同时勾选。
- 比分字符串支持：`A2:A2`、`A2:A2 1队套圈`、`A2:A2 2队套圈`、`A2:A2 1队套圈 2队套圈`。

### 3. 排行榜门槛：按牌局独立配置
- 原全局唯一门槛，改为按牌局独立设置。
- 没单独设置的牌局默认门槛为 0，即所有玩家都入榜（不再强制 5 局）。
- 设置入口：首页 → 底部"设置" → 排行榜门槛。
- 交互改成"上下滑轮 picker 选牌局 + 局数输入 + 添加"，已设置的会列在下方，每条可移除恢复默认。

### 4. AI 报告功能恢复
- 上一版的 `aiReport.js` 是占位 stub，本版恢复真实 DeepSeek API 调用。
- 走 OpenAI 兼容协议（`https://api.deepseek.com/chat/completions`）。
- API Key 仅保存在用户本地 `wx.storage`，不上传任何服务器。
- ⚠️ 上线前需在小程序后台 → 开发设置 → 服务器域名 添加 `https://api.deepseek.com`。

### 5. 数据交互页补充说明
- 每个区块（数据导入 / 格式说明 / 导出 / 重置）顶部加一行简短描述。
- 导入步骤改为带编号的 ①②③，每步下方有提示文字。
- 格式说明卡片：
  - 示例改用真实姓名（"张三 / 李四"），并新增带套圈的第二行示例。
  - 新增字段对照表：日期 / 甲乙 / 获胜方 / 比分 各字段含义、可选写法。
- 重置卡片新增红色提醒条："建议先在上方"导出"中复制 CSV 保存，方便日后通过"导入"恢复，避免数据丢失。"

---

## 🐛 修复

| 类别 | 现象 | 根因 | 处理 |
|---|---|---|---|
| UI | 记录页"取消 / 保存"按钮文字偏下、不居中 | `line-height: 96rpx` 和 `display: flex; align-items: center` 同时作用，被 wx button 内置 padding 干扰 | 改为 `line-height: 1`，由 flex 接管居中 |
| 数据 | 设置页保存了 API Key 但点"生成报告"仍提示"请先配置 API Key" | `aiReport.getDeepSeekApiKey()` 写死返回空字符串 | 改为转发到 `storage.getDeepSeekApiKey()` |
| UI | 设置页"全局默认 15"和"sk-xxx"输入框被截断显示不全 | `flex` 横排 + `min-width: 220rpx` 挤压输入区域 | 改成"标签在上、输入在下"竖排布局 |

---

## 🔧 优化

- **首页 / 排行榜**：「参与 X 局以上才会出现」「参与不足 X 局」「参与 X 局以下仅供参考」三条文案的数字跟随当前牌局门槛动态显示；门槛为 0 时不显示这些提示，避免出现"参与 0 局以上"的怪话。
- **删除牌局**：会顺手清理该牌局的独立门槛设置，不留垃圾数据。
- **首页底部 / 报告配置页底部链接**：原"API 配置"统一改为"设置"。
- **导航栏标题**：原"API 配置"统一改为"设置"。
- **CSV 复制示例**：用真实姓名 + 套圈案例，更直观。
- **storage 兼容**：`MIN_GAMES_FOR_RANK` 旧常量被改为 getter，老代码引用不会报错。
- **server/ 目录**：标记为已废弃；`project.config.json.packOptions.ignore` 已配置打包时跳过该目录。

---

## 🗂 涉及文件清单

```
新增：
  CHANGELOG.md                                    本文件
  DEPLOY.md                                       部署说明
  project.config.json                             项目主配置（之前缺失）

修改：
  utils/storage.js                                门槛 / 组队方式 API
  utils/aiReport.js                               恢复 DeepSeek 调用
  pages/record/record.js / .wxml / .wxss          组队 / 套圈 / 居中
  pages/api-config/api-config.js / .wxml / .wxss / .json   设置页重做
  pages/index/index.js / .wxml                    minGames 文案变量
  pages/leaderboard/leaderboard.js / .wxml        minGames 文案变量
  pages/import/import.js / .wxml / .wxss          数据交互页文案
  pages/report-config/report-config.wxml          footer 文案

未变更：
  pages/players/  pages/tables/  pages/history/
  pages/report-list/  pages/report-view/
  utils/importCsv.js  utils/config.js
  app.js  app.json  app.wxss  sitemap.json
```

---

## ✅ 上线前自测建议

按顺序点一遍主要功能，确认没问题再提交：

1. **记录本局**
   - 切换组队方式，1队/2队占位号码是否随之改变。
   - 4 人选择互斥（同一玩家不能同时被选两次）。
   - 比分级数 + 双勾选套圈，保存后回历史记录看比分字符串是否正确。
   - 取消 / 保存按钮文字是否垂直居中。

2. **设置页**
   - 输入框完整显示，不再被截断。
   - 滑轮选牌局 + 输入局数 + 添加 → 列表新增一条。
   - 移除按钮 → 列表对应行消失。
   - DeepSeek API Key 保存。

3. **首页 / 排行榜**
   - 切换牌局，排行榜门槛和文案随之变化。
   - 牌局未设门槛时（0），所有玩家都入榜，不显示"参与 X 局以上"提示。

4. **数据交互**
   - 文案、字段对照表、重置提醒显示正常。
   - 导出 CSV 后清空，再导入，数据完整恢复。

5. **AI 报告**（开发者工具勾选"不校验合法域名"）
   - 配 Key 后选牌局、选侧重、点生成 → 返回报告且包含真实数据。
   - 不配 Key 时 → 提示"请先配置 API Key"。

---

## 📤 上传体验版 / 提审

1. 微信开发者工具右上角「上传」→ 版本号填 **1.20**，备注复制本文件顶部「项目备注」框内容。
2. 小程序后台 → 版本管理 → 把开发版本设为体验版，验证以上五项。
3. 通过后「提交审核」。如审核驳回提示"含 AI 不可上传"，按 `DEPLOY.md` 第六节"提交审核备注"的模板回申诉。

---

## 📜 历史版本

### v1.10（上一版）
- 基础对局记录、参与人 / 牌局管理、CSV 导入导出、首页排行榜。
- AI 报告功能因审核问题暂停。
- 全局排行榜门槛固定为 5 局。

